version: "3.8"
services:
  db:
    image: postgres:15.3-alpine
    environment:
      POSTGRES_DB: rentease_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
